import 'package:flutter/material.dart';

class AddTechNotePage extends StatelessWidget {
  const AddTechNotePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Add TechNotes")));
  }
}
