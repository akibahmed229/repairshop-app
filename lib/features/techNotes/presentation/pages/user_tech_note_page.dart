import 'package:flutter/material.dart';

class UserTechNotePage extends StatelessWidget {
  const UserTechNotePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Add TechNotes'));
  }
}
